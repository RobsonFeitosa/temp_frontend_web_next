import { styled } from '@alosix-hub-ui/react'

export const OptionsFieldsContainer = styled('div', {
  display: 'block',
})

export const FieldContainer = styled('div', {
  display: 'block',
  paddingBottom: '$4',
})

export const FieldBox = styled('div', {
  display: 'block',
  maxWidth: '480px',
})
